# Database
# Default to SQLite for zero setup overhead, can be swapped to PostgreSQL later
DATABASE_URL=sqlite:///./ecosphere.db
# DATABASE_URL=postgresql://user:password@localhost:5432/ecosphere

# JWT Settings
SECRET_KEY=ecosphere_super_secret_key_placeholder
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=10080
